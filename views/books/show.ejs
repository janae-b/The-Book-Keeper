<%- include('../partials/header') %>

<section id="show-page">
  <div>Title: </div>
  <div><%= book.title %></div>
  <div>Author: </div>
  <div><%= book.author %></div>
  <div>Have you read this before?: </div>
  <div><%= book.readAgain ? 'Yes' : 'No' %></div>
</section>

<h2>Reviews</h2>
<form id="add-review-form" method="POST" action="/books/<%= book._id %>/reviews">
  <label>Review:</label>
  <textarea name="comment"></textarea>
  <label>Rating:</label>
  <select name="rating">
    <option value="1">1</option>
    <option value="2">2</option>
    <option value="3">3</option>
    <option value="4">4</option>
    <option value="5">5</option>
  </select>
  <input type="submit" value="Add Review">
</form>

<% if (book.reviews.length) { %>
  <table>
    <thead>
      <tr>
        <th>Date</th>
        <th>Review</th>
        <th>Rating</th>
      </tr>
    </thead>
    <tbody>
      <% let total = 0 %>
      <% book.reviews.forEach(function(r) { %>
        <% total += r.rating %>
        <tr>
          <td><%= r.createdAt.toLocaleDateString() %></td>
          <td><%= r.comment %></td>
          <td><%= r.rating %></td>
        </tr>
      <% }); %>
      <tr>
        <td colspan="2"></td>
        <td>
          <strong><%= (total / book.reviews.length).toFixed(1) %></strong>
        </td>
      </tr>
    </tbody>
  </table>
<% } else { %>
  <h5>No Reviews Yet</h5>
<% } %>

<h3>Would you like to update your book and review?</h3>
<form action="/books/<%= book._id %>?_method=PUT" method="POST">
  <input type="text" value="<%= book.title %>" name="title" placeholder="Title">
  <input type="text" value="<%= book.author %>" name="author" placeholder="Author">
  <input type="checkbox" <%= book.readAgain ? 'checked' : '' %> name="readAgain">Have you read this before?<br><br>
  <input type="text" value="<%= book.reviews.comment %>" name="comment" placeholder="New Review">
  <input type="number" min="1" max="5" value="<%= book.reviews.rating %>" name="rating" placeholder="New Rating">
  <button type='submit'>Update Book Info</button>
</form>

<%- include('../partials/footer') %>