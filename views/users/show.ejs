<%- include('../partials/header') %>

<h1> 📖<%= title %>📖</h1>

<div class="card-user" style="width: 18rem;">
  <img 
		id="avatarPhoto"
		style="height: 18rem;"
		class="card-img-top"
		src="<%= userInfo.avatar %>" 
		alt="Card image cap">
  <div class="card-header"></div>
  <div class="card-body">
    <h2 class="card-title"><%= userInfo.nickname %></h2><br>
    <p class="card-text">
			Joined: <%= userInfo.createdAt.toLocaleString().split(',')[0] %>
		</p>
  </div>


<%- include('../partials/footer') %>