<%- include('../partials/header') %>

<h1>Our Readers</h1>
<% users.forEach(u => { %>
    <div class="card-message">
      <div class="card-body">
        <img src="<%= u.avatar %> " class ="avatar-photo-med" alt="">
        <%= u.name %> - Joined on 
        <%= u.createdAt.toLocaleString().split(',')[0] %>
      </div>
    </div>
  </a><br>
<% }) %>


<%- include('../partials/footer') %>